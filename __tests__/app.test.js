'use strict';

// const app = require('../app.js');

jest.mock('fs');

describe('App module', () => {
  describe('Read File', () => {
    it('Should return the data in string format', () => {

      expect(true).toBeTruthy();
    });
  });
});